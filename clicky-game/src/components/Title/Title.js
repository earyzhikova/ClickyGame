import React from "react";
import "./Title.css";


const Title = () => (
<div className="App-title">
<div className="container">
  <h2 className="display-4">Clicky Vegas Memory Game!</h2>
  <p className="lead">Click on an image to earn points, but don't click on the same image twice!</p>
  </div>
</div>

)

export default Title;